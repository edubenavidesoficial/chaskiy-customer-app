enable-experiment:
  - extension-methods
analyzer:
  exclude:
    - build/**
    # Paquetes locales se validan por separado; sus reglas no deben contaminar
    # el análisis de la aplicación.
    - third_party/**
  errors:
    deprecated_member_use: ignore
